services:
  app:
    build:
      context: .
    ports:
      - "127.0.0.1:3600:3600"
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    tty: true
    command: sh -c "go run cmd/shiftmanager/main.go"
